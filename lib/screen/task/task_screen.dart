import 'package:flutter/material.dart';

class Taskcreen extends StatelessWidget {
  const Taskcreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Taskcreen'),
    );
  }
}
